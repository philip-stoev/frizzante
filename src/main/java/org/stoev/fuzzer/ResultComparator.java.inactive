package org.stoev.fuzzer;

import java.sql.Connection;

public final class ResultComparator {
	final  List<Connection> oracles = new ArrayList<Connections>();

	ResultComparator(Connections... oracles) {
		this.oracles = Arrays.asList(oracles);
	}

	ComparisonResult compare(
}
